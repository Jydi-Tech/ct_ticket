export const environment = {
    production: true,
    apiUrl: 'http://ct_ticket.local/api/CRUD',
    imageUrl: 'assets/images' //for ./deploy.sh it needs the / at the beginning used for apache2 deployment
  };
